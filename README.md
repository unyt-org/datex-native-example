# DATEX Native Example Project

This is an example project that demonstrates how to use the DATEX runtime in a native Rust application.
You can run this example with `cargo run`.
To specify a log level, use the `RUST_LOG` environment variable, e.g. `RUST_LOG=info cargo run` - by default, the log level is set to `warn`.